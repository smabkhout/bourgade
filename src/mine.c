#include "mine.h"
#include "position.h"

void place_mine(struct cell_t* cell, struct mine_t* mine){
    cell->mine=mine;
}
 srand(seed)

struct mine_t* construct_mine(char* name, enum resource_t r){

}